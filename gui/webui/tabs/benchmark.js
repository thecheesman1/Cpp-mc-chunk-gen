// Benchmark tab — comparative table is in HTML, add run button
document.addEventListener('DOMContentLoaded', () => {
    // The benchmark table is static HTML. We could add a refresh button
    // to re-run benchmarks in the future.
    console.log('Benchmark tab ready. Static comparative data displayed.');
});